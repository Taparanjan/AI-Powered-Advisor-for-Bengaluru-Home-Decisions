services:
  - type: web
    name: ai-powered-advisor-bengaluru-home-decisions
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    autoDeploy: true
